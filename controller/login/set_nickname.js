const router = require('express').Router();
const fetch = require('node-fetch');


    
// '/login/set_nickname'
router.post('/', async (req,res) => {
    // 프론트에서 Post => { accesstoken : accesstoken, nickname : "dfdfdf"}
    // 만들고, nickname, id보내주기 {nickname : "n", id : 123}
        
})

module.exports = router;